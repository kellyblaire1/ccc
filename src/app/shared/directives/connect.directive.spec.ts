import { ConnectDirective } from './connect.directive';

describe('ConnectDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectDirective();
    expect(directive).toBeTruthy();
  });
});
